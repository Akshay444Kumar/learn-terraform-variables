# test/main_test.tf

# Define a test block
/*
test "example_test" {
    
  # Define variable directly within the test file
  variable "instance_type" {
    default = "t2.small"
  }

  run "run_block_1" {
    # Define variable within a run block
    variable "instance_type" {
      default = "t2.micro"
    }
    
    # Actual test steps (e.g., terraform apply, terraform plan, assertions)
  }

  run "run_block_2" {
    # Another run block, different value for instance_type
    variable "instance_type" {
      default = "t3.medium"
    }
    
    # Actual test steps (e.g., terraform apply, terraform plan, assertions)
  }
}
*/